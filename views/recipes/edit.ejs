<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit <%= recipe.name %></title>
</head>
<body>
    <%- include('../partials/_navbar') %>
    <h1>Edit <%= recipe.name %></h1>
    <form action="/recipes/<%= recipe._id %>?_method=PUT" method="POST">
        <label for="name">Recipe name:</label>
        <input type="text" name="name" value="<%= recipe.name %>">
        <br><br>
        <label for="instructions">Instructions:</label>
        <textarea name="instructions" id="instructions"><%= recipe.instructions %></textarea>
        <br><br>
        <fieldset>
            <legend>Choose your ingredients:</legend>
            <% ingredients.forEach(ingredient => { %>
                <div>
                    <input type="checkbox" name="ingredients" value="<%= ingredient._id %>" <%= recipe.ingredients.includes(ingredient._id) ? 'checked' : '' %>>
                    <label for="ingredients"><%= ingredient.name %></label>
                </div>
            <% }); %>
        </fieldset>
        
        <br><br>
        <button type="submit">Update</button>
    </form>
    
</body>
</html>